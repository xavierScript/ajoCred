{
  "name": "contracts",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:baseSepolia": "dotenv -e .env -- hardhat run scripts/deploy.ts --network baseSepolia",
    "register:baseSepolia": "dotenv -e .env -- hardhat run scripts/registerAndSetRule.ts --network baseSepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ignition": "^3.1.8",
    "@nomicfoundation/hardhat-toolbox-viem": "^5.0.7",
    "@types/node": "^22.20.1",
    "dotenv-cli": "^11.0.0",
    "forge-std": "github:foundry-rs/forge-std#v1.9.4",
    "hardhat": "^3.12.0",
    "typescript": "~6.0.3",
    "viem": "^2.55.10"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
